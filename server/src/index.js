const server = require("./app.js");
const PORT = 3001;

server.listen(PORT, () => {
   console.log('Server raised in port: ' + PORT);
});











/*
-data.js es nuestra api.
-.pop() = .at(-1)

-find o filter

-postman, ThunderClient (plug in de vsc)
 permitira hacer request por el metodo post 
 el navegador solo hace get.

 pasarle la url 
*/